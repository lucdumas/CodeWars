// Given the string representations of two integers, return the string representation of the sum of those integers.


function sumStrings(a,b) { 
 let aToNum= Number(a)
  let bToNum=Number(b)
  
 return (aToNum + bToNum).toString()
}
